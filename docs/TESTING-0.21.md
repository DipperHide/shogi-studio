# 0.21 验证记录

本轮调整分析报告的阶段条：按参考布局分开棋手行、胜者奖杯、比例评分段和可换行摘要，保留现有分析、回放与续下行为。没有将旧版的 3D 连续帧和赛事联网测试重复计入本轮。

共 **1,430 项检查通过，0 项失败**。

| 范围 | 检查数 |
|---|---:|
| 阶段条布局、百分比边界、长姓名、触摸和键盘 | 346 |
| 完整报告、筛选、阶段与准确率入口 | 128 |
| 准确率详情、长图滚动与棋盘定位 | 121 |
| 双方阶段详情、真实定式和唯一合法着 | 451 |
| 选中着手、候选根局面、动画与续下 | 92 |
| 报告摘要、停止续跑和关键时刻 | 142 |
| Windows 成品、引擎与新阶段条 | 91 |
| APK 内容、签名、架构与对齐 | 59 |

完整 140 手历史棋谱由实际 YaneuraOu 引擎重新分析，保存 141 个局面样本。阶段条在 360×760、393×852、852×393、1100×800 和明暗两主题下检查；窄屏指标自动换成两行，宽屏一行。另测百分比舍入、99.9% 边界、超长姓名、缺失棋手／阶段、胜者标记、触摸及键盘入口、返回后的着手选择。

测试初次运行暴露出旧测试在页面重建后过早取滚动位置的问题，已改为等待布局并滚动到具体按钮。截图复核另发现亮色模式下新说明页的返回按钮对比度不足，已修复通用木纹按钮样式并加入回归检查。最终成功运行日志单独记录，未把首次失败当作通过。

成品验证确认 APK 为非 debuggable 的 release 构建、versionCode 39、原包名和相同签名、ARM64 与 16 KB 对齐。37 枚界面图标的源文件、导入纹理及 APK 内容核对一致，新阶段条字节码存在，会员资源缺席。Windows ZIP 的程序散列与实际运行的成品相同；首帧棋盘在 2553 毫秒绘制。

原始日志、截图、引擎样本和构建收据位于本机 `review/app/chessis21/`，公开摘要为 `docs/releases/v0.21.0-validation.json`。源码及产物散列逐项核对通过；旧版成品和用户数据保留。

本轮未连接 Android 真机，也未运行缺少分包的原版 APK。等级分未校准，显示“—”。测试不能保证绝对零 bug，全应用 UI 和全部算法仍有实现差距。参考依据见 `docs/REPORT-PHASE-RIBBON.md`，原有棋盘／赛事检查见 `docs/TESTING-0.20.md`。
