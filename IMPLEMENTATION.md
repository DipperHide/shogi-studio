# 完整将棋实施与验收记录

目标：在极简竖屏将棋中加入网络与蓝牙对战、实际 YaneuraOu 引擎及可调难度、辅助与分析、完整可用的相关界面，以及用户提供两本书的互动教程。此记录用于延续工作，不缩小原目标。

## 必须完成并提供证据

- [ ] YaneuraOu：Windows 和 Android 实际可执行引擎，USI 握手、局面与历史、设置难度、合法应手、停止与恢复、崩溃和超时处理；引擎来源、对应源码、模型和许可证随包保留。
- [x] 辅助与分析：提示着手、多候选评分和变化线、棋谱任意局面分析，切换页面/新局/悔棋时不应用过期结果。
- [ ] 网络对战：创建与加入、先后手、同步合法着手、掉线与恢复、退出/认输/再战、局面冲突校验；两独立客户端实测。互联网连接路径和局域网连接路径须分别验证，不把仅本机测试描述为公网可用。
- [ ] 蓝牙：Android 权限、设备列表、配对/连接、双向棋局协议、断线/重连和错误状态；真实蓝牙设备验收独立记录，不能以虚拟传输替代。
- [x] 完整界面：开局/续局/对弈配置/难度/联机/蓝牙/棋谱/分析/教程/设置/终局；保持棋盘只有线与文字，功能从按需打开的文字菜单进入。
- [x] 完整对局：计时、认输、悔棋、翻转、终局与声明、存档和棋谱管理，网络模式遵守双方协议，不允许本地操作改变远端局面。
- [x] 两本书的全部内容逐章转为教程：原始页索引、章节覆盖、图中棋局、可操作练习、解答反馈、复习与进度。读页器或少量示例不等于完成全书互动化。
- [ ] 新 Android APK 与 Windows 构建、实际启动、界面/规则/协议/引擎/教程验证，无已知必需功能空壳。

## 当前证据

2026-09-07 复查：目前已合并为一个 App，两种外观使用相同对局控制、菜单、棋谱、引擎、计时与联机。原极简与木制旧报告不证明新增目标完成；合并界面证据在 `review/app/unified/`，本任务新增证据在 `review/app/complete/` 和 `review/app/tutorial/`。

| 目标 | 目前状态与证据 |
|---|---|
| YaneuraOu | 已接入真实 V9.00 + Suisho5 NNUE、六档搜索预算、取消和异常处理；Windows 实际 USI 与成品应手通过。Android 原生引擎已编译并打包，尚未真机运行。见 `complete/usi-tests.json` 和成品 probe |
| 分析与棋谱 | 三个候选、评分和变化线，本局和独立档案任意步分析；统一 JSON 导入导出、重命名、删除、继续指定局面、旧档迁移。见 `unified/ui-tests.json` |
| 对局与界面 | 两种外观、中日英、明暗、移动/升变/打入/悔棋/声明/计时/终局；统一界面 339 项通过，1688 项翻译与字形检查通过。见 `unified/ui-tests.json` |
| 网络 | 本轮双独立客户端、不同外观与语言 33 + 28 项通过。支持协议协商、主持方计时、断线恢复；只证明本机 TCP，不代表两台局域网设备或公网已实测 |
| 联机存档 | 新增原子保存、合法备份、身份与完整棋谱一起恢复。`complete/network-store-tests.json` 22 项、`network-restore-tests.json` 22 项通过；重启后实际 TCP 继续行棋，坏档恢复，退出不复活旧连接，已连接时重复重连保持连接 |
| 蓝牙 | 权限、发现、配对列表、RFCOMM 与对局协议已接入，连接失败资源释放已修补，插件编译通过；真实蓝牙双机尚未验收 |
| 教程 | 两册全部编入，共 232 课、1357 步、1010 道互动题。入门 222 教学页+5 非教学页；序盘 217 教学页+10 非教学页，草稿/待制作均为0。全部步骤与实际答案模拟通过，框架77项、字体4384项通过。Windows成品两册可打开、回放答案并保存进度 |

用户已提供两本本地 EPUB：《改訂版 羽生善治の みるみる強くなる 将棋入門》和《改訂版 羽生善治の みるみる強くなる 将棋入門 序盤の指し方》。文件位于用户 Downloads；实际每本 **227 页**，已完整提取，共 454 页。来源文件只读，扫描页与 OCR 留在 `course_sources/`，不直接随 App 打包。

两册课程在 `godot/courses/hanyu-intro.json` 与 `hanyu-opening.json`，每张源页的教学、例图、问题、专栏和术语均已逐页核对，并在 `coverage` 映射课节。原始 OCR 和 `recognized-diagrams.json` 仍保留为未审候选，不作为验收证据。入门含两盘完整将死实战、12 道诘题和原书24条错解、97个术语；序盘含全部52幅第4章图及展开的必至/将死验证。源书两处物料异常（序盘索引98缺一步、160第8图多一步）忠实保留并记录，未改动正常对局规则。逐页报告见 `review/app/complete/tutorial-intro-coverage.md`、`tutorial-opening-coverage.md`。

最终课程验证见 `review/app/tutorial/tests.json`；记录实际测试的两份课程 SHA-256。打包清单 `godot/courses/manifest.json` 与成品检查比对相同散列，确保 Windows、Android 含相同完整版本。课程导航支持中日英，正文中文；修复日文界面缺少简体字的字体回退及语言切换时字体提前释放。

## 继续工作与验收边界

- 两本书全课程、错题复习、进度重试、逐手示范及小屏操作已通过。Windows / Android 交付版本为0.6.1（Android versionCode21），成品证据与散列见 `review/app/complete/build/`。
- Android 真机：安装、引擎、系统文档选择器、触屏与蓝牙权限/扫描。用户目前只有一台手机，电脑尚未识别到授权调试设备。单机验证脚本 `scripts/test_android_device.ps1` 已准备且语法检查通过，**未在设备执行**。
- 两台 Android 的实际蓝牙对战和重连，需要第二台设备，尚无法实测。
- 两台局域网设备与跨网络公网直连分别验收。保留可达 IP / 端口的直连方式，不新增匹配大厅或服务器。

YaneuraOu V9.00 官方 Windows 发布包已下载并与 GitHub 公布的 SHA-256 一致：`6517997dd05ba049a2244a828216967a0ad351d975ec52a0f358e2883197dec6`。对应源码、构建脚本与 GPL 许可证随包保留。Suisho5 原包未附独立模型许可证文件，此事实和来源记录于 `engines/yaneuraou/NOTICE.md`，不擅自将模型标注成 CC0、MIT 或引擎 GPL。
